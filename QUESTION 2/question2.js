function delayedGreeting(name) {
    setTimeout(() => {
      console.log(`Hello, ${name}!`);
    }, 2000); // Delay of 2 seconds (2000 milliseconds)
  }
  
  // Example usage:
  delayedGreeting("Roddy");
  
  
  
  